Invoke with:

sbt run sourceip sourceport destip destport



Example use case:

1. Set up receiving server with
nc -l 8011

2. Run our app with
sbt run localhost 8010 localhost 8011

3. Run nc and start the int sending with
nc localhost 8010
1
